import React from 'react';
import {css} from "@emotion/react";

function App() {
  return (
    <div className="App" css={mainCss}>
      hello world!!!
    </div>
  );
}

const mainCss = css`
  
`;

export default App;
